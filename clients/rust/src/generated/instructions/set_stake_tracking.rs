//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const SET_STAKE_TRACKING_DISCRIMINATOR: u8 = 5;

/// Accounts.
#[derive(Debug)]
pub struct SetStakeTracking {
    pub whitelist_signer: solana_pubkey::Pubkey,

    pub whitelist: solana_pubkey::Pubkey,
}

impl SetStakeTracking {
    pub fn instruction(
        &self,
        args: SetStakeTrackingInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: SetStakeTrackingInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            self.whitelist_signer,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.whitelist, false));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = SetStakeTrackingInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::JITO_WHITELIST_MANAGEMENT_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SetStakeTrackingInstructionData {
    discriminator: u8,
}

impl SetStakeTrackingInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 5 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for SetStakeTrackingInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct SetStakeTrackingInstructionArgs {
    pub total_stake_deposited: u64,
    pub total_stake_withdrawn: u64,
    pub total_withdrawal_fees: u64,
}

impl SetStakeTrackingInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `SetStakeTracking`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` whitelist_signer
///   1. `[writable]` whitelist
#[derive(Clone, Debug, Default)]
pub struct SetStakeTrackingBuilder {
    whitelist_signer: Option<solana_pubkey::Pubkey>,
    whitelist: Option<solana_pubkey::Pubkey>,
    total_stake_deposited: Option<u64>,
    total_stake_withdrawn: Option<u64>,
    total_withdrawal_fees: Option<u64>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SetStakeTrackingBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn whitelist_signer(&mut self, whitelist_signer: solana_pubkey::Pubkey) -> &mut Self {
        self.whitelist_signer = Some(whitelist_signer);
        self
    }
    #[inline(always)]
    pub fn whitelist(&mut self, whitelist: solana_pubkey::Pubkey) -> &mut Self {
        self.whitelist = Some(whitelist);
        self
    }
    #[inline(always)]
    pub fn total_stake_deposited(&mut self, total_stake_deposited: u64) -> &mut Self {
        self.total_stake_deposited = Some(total_stake_deposited);
        self
    }
    #[inline(always)]
    pub fn total_stake_withdrawn(&mut self, total_stake_withdrawn: u64) -> &mut Self {
        self.total_stake_withdrawn = Some(total_stake_withdrawn);
        self
    }
    #[inline(always)]
    pub fn total_withdrawal_fees(&mut self, total_withdrawal_fees: u64) -> &mut Self {
        self.total_withdrawal_fees = Some(total_withdrawal_fees);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = SetStakeTracking {
            whitelist_signer: self.whitelist_signer.expect("whitelist_signer is not set"),
            whitelist: self.whitelist.expect("whitelist is not set"),
        };
        let args = SetStakeTrackingInstructionArgs {
            total_stake_deposited: self
                .total_stake_deposited
                .clone()
                .expect("total_stake_deposited is not set"),
            total_stake_withdrawn: self
                .total_stake_withdrawn
                .clone()
                .expect("total_stake_withdrawn is not set"),
            total_withdrawal_fees: self
                .total_withdrawal_fees
                .clone()
                .expect("total_withdrawal_fees is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `set_stake_tracking` CPI accounts.
pub struct SetStakeTrackingCpiAccounts<'a, 'b> {
    pub whitelist_signer: &'b solana_account_info::AccountInfo<'a>,

    pub whitelist: &'b solana_account_info::AccountInfo<'a>,
}

/// `set_stake_tracking` CPI instruction.
pub struct SetStakeTrackingCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub whitelist_signer: &'b solana_account_info::AccountInfo<'a>,

    pub whitelist: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: SetStakeTrackingInstructionArgs,
}

impl<'a, 'b> SetStakeTrackingCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: SetStakeTrackingCpiAccounts<'a, 'b>,
        args: SetStakeTrackingInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            whitelist_signer: accounts.whitelist_signer,
            whitelist: accounts.whitelist,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.whitelist_signer.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.whitelist.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.1,
                is_writable: remaining_account.2,
            })
        });
        let mut data = SetStakeTrackingInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::JITO_WHITELIST_MANAGEMENT_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.whitelist_signer.clone());
        account_infos.push(self.whitelist.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `SetStakeTracking` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` whitelist_signer
///   1. `[writable]` whitelist
#[derive(Clone, Debug)]
pub struct SetStakeTrackingCpiBuilder<'a, 'b> {
    instruction: Box<SetStakeTrackingCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SetStakeTrackingCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(SetStakeTrackingCpiBuilderInstruction {
            __program: program,
            whitelist_signer: None,
            whitelist: None,
            total_stake_deposited: None,
            total_stake_withdrawn: None,
            total_withdrawal_fees: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn whitelist_signer(
        &mut self,
        whitelist_signer: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.whitelist_signer = Some(whitelist_signer);
        self
    }
    #[inline(always)]
    pub fn whitelist(&mut self, whitelist: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.whitelist = Some(whitelist);
        self
    }
    #[inline(always)]
    pub fn total_stake_deposited(&mut self, total_stake_deposited: u64) -> &mut Self {
        self.instruction.total_stake_deposited = Some(total_stake_deposited);
        self
    }
    #[inline(always)]
    pub fn total_stake_withdrawn(&mut self, total_stake_withdrawn: u64) -> &mut Self {
        self.instruction.total_stake_withdrawn = Some(total_stake_withdrawn);
        self
    }
    #[inline(always)]
    pub fn total_withdrawal_fees(&mut self, total_withdrawal_fees: u64) -> &mut Self {
        self.instruction.total_withdrawal_fees = Some(total_withdrawal_fees);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = SetStakeTrackingInstructionArgs {
            total_stake_deposited: self
                .instruction
                .total_stake_deposited
                .clone()
                .expect("total_stake_deposited is not set"),
            total_stake_withdrawn: self
                .instruction
                .total_stake_withdrawn
                .clone()
                .expect("total_stake_withdrawn is not set"),
            total_withdrawal_fees: self
                .instruction
                .total_withdrawal_fees
                .clone()
                .expect("total_withdrawal_fees is not set"),
        };
        let instruction = SetStakeTrackingCpi {
            __program: self.instruction.__program,

            whitelist_signer: self
                .instruction
                .whitelist_signer
                .expect("whitelist_signer is not set"),

            whitelist: self.instruction.whitelist.expect("whitelist is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct SetStakeTrackingCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    whitelist_signer: Option<&'b solana_account_info::AccountInfo<'a>>,
    whitelist: Option<&'b solana_account_info::AccountInfo<'a>>,
    total_stake_deposited: Option<u64>,
    total_stake_withdrawn: Option<u64>,
    total_withdrawal_fees: Option<u64>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
